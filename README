#API untuk service
API ini digunakan untuk menyimpan dan melihat histori dari data user yang akan diupload
Untuk sekarang, API ini hanya terdiri dari 3 Endpoint

##Endpoint pertama
Endpoint yang pertama adalah "/". Endpoint yang ini menggunakan metode GET. Hasil akhirnya adalah mengembalikan response bahwa Endpoint berhasil Terhubung

##Endpoint kedua
Endpoint yang kedua adalah "/insertImage". Endpoint ini menggunakan metode POST. Hasil akhirnya adalah mengembalikan respon seperti "Insert Successfully"

Untuk menggunakan ini. masukan 2 variabel. Yang pertama bernama "image" yang berisi file gambar x-ray. Yang kedua adalah username pengguna. Karena belum ada fitur user. isi username dengan "hellow" dahulu.

##Endpoint ketiga
Endpoint yang ketiga adalah "/history/:username". Endpoint ini menggunakan metode GET. Hasil akhirnya adalah mengembalikan semua history dari user yang berada didalam database **dalam bentuk object**

Gunakan 1 variabel yang bernama "username". isi dari username adalah username dari pengguna. Untuk sekarang, gunakan nama username "hellow" karena masih belum ada fitur login/register.